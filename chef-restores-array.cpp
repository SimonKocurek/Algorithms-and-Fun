#include <bits/stdc++.h>

using namespace std;

int main() {
    int sets;
    cin >> sets;

    for (int i = 0; i < sets; ++i) {
        int s, rest, max_s;
        cin >> s >> rest >> max_s;

        int arr[s];
        for (auto& a : arr) {
            cin >> a;
        }

        for (int j = 0; j < rest; ++j) {
            
        }
    }
    return 0;
}