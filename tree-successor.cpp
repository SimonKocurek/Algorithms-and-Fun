#include <bits/stdc++.h>

using namespace std;

struct Node {
  int value;
  Node* left;
  Node* right;
  Node* parent;

  Node(int value, Node* left, Node* right) : value(value), left(left), right(right) {}
};

Node* Successor(Node* node) {
}

int main() {
  return 0;
}

