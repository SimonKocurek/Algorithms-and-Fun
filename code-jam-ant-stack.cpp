#include <bits/stdc++.h>

using namespace std;

void Solve() {
    int ant_count;
    cin >> ant_count;

    vector<int> ants(ant_count);
    for (auto& ant : ants) {
        cin >> ant;
    }

    
}

int main() {
    int tests;
    cin >> tests;

    for (int i = 1; i <= tests; ++i) {
        cout << "Case #" << i << ": ";
        Solve();
        cout << "\n";
    }

    return 0;
}
