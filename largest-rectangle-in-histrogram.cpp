#include <bits/stdc++.h>

using namespace std;

typedef pair<int, int> pii;

pii largest_rectangle(vector<int> &histogram)
{
    if (histogram.size() <= 1)
    {
        return {0, 0};
    }

    
}

int main()
{
    return 0;
}
