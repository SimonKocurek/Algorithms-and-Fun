#include <iostream>

using namespace std;

int main() {
    int sets;
    cin >> sets;
    for (int i = 0; i < sets; ++i) {
        int start, end;
        cin >> start >> end;
        
        int optimum[5001];
        optimum[start] = 0;
        for (int j = start; j <= end; ++j) {
            optimum[]
        }

    }

    return 0;
}

