#include <bits/stdc++.h>

using namespace std;

struct Node {
    unordered_set<char> edge;
};

int main() {
    int y, x, word_count;
    cin >> y >> x >> word_count;

    for (int i = 0; i < y; ++i) {
        for (int j = 0; j < x; ++j) {
            char c;
            cin >> c;

        }
    }

    for (int i = 0; i < word_count; ++i) {
        
    }
    return 0;
}
